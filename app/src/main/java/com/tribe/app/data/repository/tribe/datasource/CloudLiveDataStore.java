package com.tribe.app.data.repository.tribe.datasource;

/**
 * {@link LiveDataStore} implementation based on connections to the api (Cloud).
 */
public class CloudLiveDataStore implements LiveDataStore {
    /**
     * Construct a {@link LiveDataStore} based on connections to the api (Cloud).
     */
    public CloudLiveDataStore() {
    }
}
