<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="user.infos">
        {
            user(id: \"%1$s\") {
                ...UserInfos

                friends {
                    ...UserInfos
                }

                groups {
                    name
                }

                reported {
                    ...UserInfos
                }
            }
        }

        fragment UserInfos on User {
            id
            displayName
            profilePicture
        }
    </string>
    <!-- installs {
        gcm_id
        user {
            ...UserInfos
        }

        members {
            ...UserInfos
        }
    } -->
    <string name="user.mutate.name">
        mutation {
            updateUser(id: \"%1$s\", user:{
                displayName : "Tiago"
            })

            {
                displayName
            }
        }
    </string>
</resources>